{
  "name": "cds-website-pr-bot",
  "version": "1.0.0",
  "description": "Builds markdown content from CMS changes and opens a PR to commit new or changed files",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist"
  },
  "author": "Bethany Dunfield",
  "license": "ISC",
  "dependencies": {
    "@actions/github": "^4.0.0",
    "js-base64": "^3.6.0"
  },
  "devDependencies": {
    "dotenv": "^8.2.0"
  }
}
